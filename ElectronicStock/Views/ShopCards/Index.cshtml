@model IEnumerable<ElectronicStock.Models.ShopCard>

@{
    ViewData["Title"] = "Index";
}

<h1>Shop Card</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CreateDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Status)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CreateDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ShopCardId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ShopCardId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ShopCardId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
